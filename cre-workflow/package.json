{
  "name": "uhuru-credit-scoring",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --target=browser --outdir=dist src/credit-scoring.ts",
    "test": "bun test"
  },
  "dependencies": {
    "viem": "^2.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
